---
description: 通用 Git 提交规范和代码审查规范
globs: ["**/*"]
alwaysApply: true
---

# Git 提交规范

## Commit Message 格式

遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <subject>

[optional body]

[optional footer]
```

### 类型（type）

| 类型       | 说明                         |
| ---------- | ---------------------------- |
| `feat`     | 新增功能                     |
| `fix`      | 修复 Bug                     |
| `docs`     | 文档变更                     |
| `style`    | 代码格式（不影响逻辑）       |
| `refactor` | 重构（不新增功能，不修复Bug）|
| `perf`     | 性能优化                     |
| `test`     | 增加或修改测试               |
| `chore`    | 构建过程或辅助工具变动       |
| `ci`       | CI/CD 相关变更               |
| `revert`   | 回退某次提交                 |

### 示例

```
feat(user): add user profile page

- add avatar upload functionality
- add profile edit form with validation
- integrate with user API

Closes #123
```

```
fix(auth): resolve token refresh race condition

When multiple requests fail with 401 simultaneously, only
the first token refresh request is sent, others wait.

Fixes #456
```

## 分支命名规范

```
feature/<description>    # 新功能
fix/<description>        # Bug 修复
hotfix/<description>     # 紧急修复
release/<version>        # 发布准备
chore/<description>      # 维护任务
```

## 代码审查清单

在提交 PR 前，检查以下内容：

### 代码质量
- [ ] 代码通过所有 lint 检查
- [ ] 新增代码有对应的单元测试
- [ ] 测试覆盖率不低于现有水平
- [ ] 没有引入新的 `TODO`/`FIXME`（或已记录在 Issue 中）

### 安全性
- [ ] 没有硬编码的密钥、密码或 token
- [ ] 用户输入经过验证和净化
- [ ] 敏感信息不写入日志
- [ ] 依赖没有已知安全漏洞

### 性能
- [ ] 没有 N+1 查询问题
- [ ] 大数据集使用分页
- [ ] 耗时操作考虑了超时处理

### 文档
- [ ] 公共 API 有注释说明
- [ ] README 或文档已更新（如有必要）
- [ ] CHANGELOG 已更新（如有必要）
